<form action="<%= track_notes_url(@track) %>" method="POST">
  <textarea name="track[note]" id="note_id"></textarea>
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">

  <br>
  <input type="submit">

</form>
